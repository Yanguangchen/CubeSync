These files are the selector options for the dropdown menu/autocomplete inputs used by the CubeSync request forms.

Each text file contains one option per line. `app.js` fetches them from `dropdown-options/` and merges them with saved browser entries from `localStorage`.
