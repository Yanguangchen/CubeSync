{
  "name": "cubesync-form",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/write-env.js",
    "lint": "eslint .",
    "test": "node --test --experimental-test-coverage",
    "test:firestore-rules": "firebase emulators:exec --only firestore --project cubesync-rules-test \"node --test firestore-rules-emulator.test.js\"",
    "validate-env": "node scripts/validate-env.js"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "vercel": "^54.17.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@firebase/rules-unit-testing": "^5.0.1",
    "eslint": "^9.39.4",
    "firebase": "^12.15.0",
    "jsdom": "^22.1.0"
  }
}
